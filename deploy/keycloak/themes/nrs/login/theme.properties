# NRS login theme — extends Keycloak's stock login theme and layers ROP branding on top.
# Keeps the base login.css (falls back to the parent) and adds custom.css for our palette.
parent=keycloak
import=common/keycloak
styles=css/login.css css/custom.css
